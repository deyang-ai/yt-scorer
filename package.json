{
  "name": "yt-scorer",
  "version": "1.0.0",
  "description": "Chrome extension that scores YouTube videos by comment sentiment",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
